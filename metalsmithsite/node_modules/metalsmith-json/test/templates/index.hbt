<!DOCTYPE html>
<html>
    {{#each collections.builds}}
        <div class='build'>
            <h2>Build #{{foo.build}}</h2>
            <p>version: {{foo.version}}</p>
        </div>
    {{/each}}
</html>

